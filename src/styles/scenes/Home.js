/* @flow */

import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    color: '#666666',
    fontSize: 16,
    margin: 36,
  },
});

export default styles;
