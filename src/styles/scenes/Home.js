/* @flow */

import {
  Dimensions,
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    color: '#AAAAAA',
    fontSize: 16,
    textAlign: 'center',
  },
  details: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  camera: {
    flex: 3,
    alignSelf: 'stretch',
  },
});

export default styles;
